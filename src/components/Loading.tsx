import React from "react";
import "../components/css/Loading.css"; // Import CSS for styling

const Loading = () => {
  return (
    <div className="loading">
      <div className="spinner"></div>
    </div>
  );
};

export default Loading;
